import { Typography } from '@mui/material';
import { color } from '@mui/system';
import React from 'react'

const CRUDCreator = (excelFile) => {
    const values = [];

    return (
        <Typography sx={{color:'text.primary', backgroundColor:'text.secondary'}}>Componente CRUDCreator</Typography>
    )
}

export default CRUDCreator